@namespace BlazorQL.Sample
@using Microsoft.AspNetCore.Components.Routing
@using BlazorQL

<Router AppAssembly="typeof(App).Assembly">
    <Found Context="routeData">
        <RouteView RouteData="routeData" />
    </Found>
    <NotFound>
        <p class="route-missing">There is nothing at this address. <a href=".">Back to the sample</a>.</p>
    </NotFound>
</Router>
@* Outside the router so the debug panel is on every page. *@
<BlazorQLSidecar />
