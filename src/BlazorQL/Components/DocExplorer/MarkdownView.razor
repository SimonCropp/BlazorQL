@namespace BlazorQL

@if (html is not null)
{
    <div class="blazorql-markdown @(Preview ? "blazorql-markdown-preview" : null)">@((MarkupString)html)</div>
}
else if (Fallback is not null)
{
    <div class="blazorql-markdown blazorql-markdown-fallback">@Fallback</div>
}
