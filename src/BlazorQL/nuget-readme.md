# BlazorQL

An in-browser GraphQL IDE for Blazor — a GraphiQL alternative. Monaco editors via BlazorMonaco with schema-aware editing (completion, validation, hover) computed in C#, a documentation explorer, tabs, history, variables and headers editors, subscriptions, and incremental delivery.

See https://github.com/SimonCropp/BlazorQL for documentation.
