# BlazorQL

An in-browser GraphQL IDE for Blazor — a GraphiQL alternative. Schema-aware editing (completion, validation, hover) via monaco-graphql, a documentation explorer, tabs, history, variables and headers editors, subscriptions, and incremental delivery.

See https://github.com/SimonCropp/BlazorQL for documentation.
