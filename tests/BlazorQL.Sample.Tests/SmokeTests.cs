[TestFixture]
public class SmokeTests
{
    // Placeholder until the browser fixture lands: proves the test pipeline itself.
    [Test]
    public void TestPipelineRuns() =>
        Assert.That(true, Is.True);
}
